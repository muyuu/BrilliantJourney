// @flow
import React from 'react';
import {render} from 'react-dom';

const Map = (props: {siteInfo: {name: string}}) => {
    return (
        <div className="map">
        </div>
    );
};

export default Map;
