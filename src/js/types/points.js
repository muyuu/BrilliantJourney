// @flow

export type PointAction = {
    type: string,
    id: string,
    place: string,
    arrivedAt: number,
    duration: number,
}
export type PointObj = {
    id: string,
    place: string,
    arrivedAt: number,
    duration: number,
}
