const initialSiteInfo = {
    name: "brilliantJourney",
};

export const siteInfo = (state = initialSiteInfo, action) => {
    switch(action.type){
        default:
            return state;
    }
};


